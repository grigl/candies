<%= form_for @order, :url => update_checkout_path(@order.state), :html => { :id => "checkout_form_#{@order.state}" }, remote: true do |form| %>
    <%= render @order.state, :form => form %>
    <input id="post-final" type="submit" style="display:none"/>
<% end %>

<%= link_to 'Далее', update_checkout_path(@order.state), remote: true, :class => 'button' %>
<%= @order.inspect %>