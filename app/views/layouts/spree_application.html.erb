<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">

  <title>Candies</title>
  <meta name="description" content="">
  <meta name="author" content="">

  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link rel="shortcut icon" href="favicon.ico">
  <link href='http://fonts.googleapis.com/css?family=Ubuntu+Condensed&subset=latin,cyrillic-ext,cyrillic' rel='stylesheet' type='text/css'>

  <%= stylesheet_link_tag 'style', 'ie' %>
  <%= javascript_include_tag 'libs/jquery-1.7.2.min',
                             'libs/jquery.ikselect.min',
                             'libs/modernizr-2.0.6.min',
                             'script.js',
                             'rails.js' %>
  <%= javascript_include_tag :defaults %>                      
  <%= csrf_meta_tag %>
  <%= yield(:head) %>
</head>
<body>

<%= render 'shared/header' %>

<div class="page white">
  <div class="wrapper">
    
    <div class="basket">
      <%= render 'shared/cart_quantity' %>
    </div>

    <div class="left-colum">
      <nav class="catalog-menu dotted">
        <div class="icon-sex"><img src="images/ico_male.png" width="55" height="67" alt="male"></div>
        <ul class="catalog-menu-first">
          <% retrieve_collections.each do |collection| %>
            <li><%= link_to collection.name, seo_url(collection) %></li>
          <% end %>
        </ul>
        <ul class="catalog-menu-second">
          <% retrieve_brands.each do |brand| %>
            <li><%= link_to brand.name, seo_url(brand) %></li>
          <% end %>
        </ul>
      </nav>
    </div>
    <div class="right-colum">
      <nav class="catalog-menu dotted">
        <div class="icon-sex"><img src="images/ico_female.png" width="55" height="67" alt="female"></div>
        <ul class="catalog-menu-first">
          <% retrieve_collections.each do |collection| %>
            <li><%= link_to collection.name, seo_url(collection) %></li>
          <% end %>
        </ul>
        <ul class="catalog-menu-second">
          <% retrieve_brands.each do |brand| %>
            <li><%= link_to brand.name, seo_url(brand) %></li>
          <% end %>
        </ul>
      </nav>
    </div>
    <!-- /catalog-menu -->

    <%= debug params %>

    <%= yield %>

  </div>
</div>

<div class="page">
  <%= render 'shared/personal_page' %>
</div>

</body>
</html>